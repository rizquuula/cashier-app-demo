Our opencode config dir is in `./.opencode`.

Always use skills whenever possible – load the relevant skill via the `skill` tool before starting work on a matching task.

Look into the `Makefile` for command shortcuts (e.g., `make run`, `make test`, `make seed`).